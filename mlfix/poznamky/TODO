- generalize scenarios (language selection)
DONE - improve NodeInfoGetter - use Alignment to determine parent/child relationships (Using parsing from ali)
DDONE - make statistics about old_node* -> new_node* category changes (see features-notes.txt)
	-> choose which categories we want to predict/change by classification ()

- parametrized generation of model config files

experimenting:
- multiple ScikitLearn models? (each predicting different subset of Iset classes)

two methods of evaluation:
- predicted categories - classification (model developement)
- BLEU


? reordering model ?:
    - based on the edgedirection prediction
    - need correct parser for reference sentences

changing agreement:
    - number, gender, case, person
    - classifier for determining whether to change (depending on the node-parent)
    - second classifier to choose wheter to chang node or parent
        - bottom down, propagate the change up?

data analysis:
    - parent;parent_ref difference -> change dependent on the parent change
    - extract for each word:
sent_id \t different_form \t different_lemma \t different_tag \t different_iset \t different_parent(all_cat) \t form;form_ref \t lemma;lemma_ref \t tag;tag_ref \t iset;iset_ref \t parent;parent_ref


INSTANCE EXTRACTION (two possibilities):
- 1) parse reference -> no projection !!!
- 2) change in agreement -> 0 => 1


unbalanced classes (wrong_form_3):
- weighting (oversampling TP)
- predict_proba + threshold
-

training:
- model choice
- param tuning
- filtering


===============

next steps:
- choose feat_select method (new_node_case)
- hyperparameter tuning

- wrong_form_3 model analysis
- wrong_form_3 feat_select + tuning
- weighting

- quick model comparison (gnc)
- quick selector comparison (gnc)
|-> no graphs - only short note
- case + number
- case + number + gender

----------------
Eval:
datasets
- autodesk
- wmt10
- wmt16-chimera
- wmt16-nmt
- himl

- depfix, no-depfix

- manual on wmt10/himl

!!! references to WMTs etc.

oracle_cat oracle_error oracle_all
- cat: case, gnca
- error: comb vs best vs other?
- all: res + manual

-----------------------
Thesis:
- check reference format
- check references in text
- Attachements - Scenario, Feature set, Examples?, ???
- List of fig., tab.?
